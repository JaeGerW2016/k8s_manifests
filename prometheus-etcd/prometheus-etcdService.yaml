apiVersion: v1
kind: Service
metadata:
  name: etcd-k8s
  namespace: kube-system
  labels:
    k8s-app: etcd
spec:
  selector:
    k8s-app: etcd
  ports:
  - name: port
    port: 2379
    protocol: TCP
